{
  "name": "club-actu",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:remote": "wrangler d1 migrations apply club-actu-db --remote",
    "db:migrate:local": "wrangler d1 migrations apply club-actu-db --local"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
